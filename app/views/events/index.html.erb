<h1>Events</h1>

<ul>
  <% @events.each do |event| %>
    <li>
      <%=link_to event.title, event_path(event)%>
    </li>
  <% end %>
</ul>
<%if user_signed_in?%>
  <p><%= link_to "Create New Event", new_event_path%></p>
<%end%>